connector.name=bigquery
# Service account JSON key mounted at /tmp/bigquery_key.json (see docker-compose.yml).
bigquery.project-id=${ENV:BIGQUERY_PROJECT_ID}
bigquery.credentials-file=/tmp/bigquery_key.json
# Uncomment if querying datasets that live in a different billing project.
# bigquery.parent-project-id=${ENV:BIGQUERY_PARENT_PROJECT_ID}
# bigquery.views-enabled=true
