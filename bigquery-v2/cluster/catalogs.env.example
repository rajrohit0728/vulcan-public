# Copy to .env and fill in
#   cp catalogs.env.example .env
#   set -a && source .env && set +a
#   docker compose up -d

# --- bigquery ---
BIGQUERY_PROJECT_ID=your-gcp-project-id
# Optional: set only if querying datasets billed to a different project
BIGQUERY_PARENT_PROJECT_ID=
